# 電商微服務系統：實戰總結

## 概述

本文以一個**電子商務平台**為例，展示如何使用本系列學到的所有 Spring Boot 2 知識，設計並實作一套微服務架構。

> 本文是本系列所有概念的**綜合應用**，建議先閱讀前面各篇文章後再閱讀本文。
>
> **另有 Spring Modulith 版本**：[[20-電商系統-Spring-Modulith-版本]] — 同一個電商系統以模組化單體（Modular Monolith）架構實作，適合比較兩種架構的差異。

---

## 系統架構圖

```
                    ┌─────────────────────────────────────────────┐
                    │              API Gateway（8080）              │
                    │        路由、認證、限流、負載均衡               │
                    └──────────────────────┬──────────────────────┘
                                           │
          ┌────────────────────────────────┼────────────────────────────────┐
          │                               │                                │
┌─────────▼─────────┐          ┌──────────▼──────────┐          ┌─────────▼─────────┐
│  User Service      │          │  Product Service      │          │  Order Service     │
│  （8081）          │          │  （8082）             │          │  （8083）          │
│                   │          │                      │          │                   │
│  - 使用者管理      │          │  - 商品管理            │          │  - 訂單管理        │
│  - 認證/授權       │          │  - 分類管理            │          │  - 訂單狀態        │
│  - JWT Token      │          │  - 庫存管理            │          │  - 訂單查詢        │
│                   │          │  - 商品快取            │          │                   │
└─────────┬─────────┘          └──────────┬──────────┘          └─────────┬─────────┘
          │                               │                                │
┌─────────▼─────────┐          ┌──────────▼──────────┐          ┌─────────▼─────────┐
│  MySQL（users）   │          │  MySQL（products）   │          │  MySQL（orders）   │
└───────────────────┘          └─────────────────────┘          └─────────┬─────────┘
                                                                           │
          ┌────────────────────────────────┐         ┌────────────────────┘
          │                               │         │
┌─────────▼─────────┐          ┌──────────▼──────────────────────▼──────┐
│  Payment Service   │          │              Message Broker             │
│  （8084）          │          │              （Kafka / RabbitMQ）       │
│                   │          │                                         │
│  - 支付處理        │          │  order.placed, payment.completed        │
│  - 退款            │          │  inventory.updated, email.send          │
│  - 支付狀態        │          │                                         │
└─────────┬─────────┘          └──────────────────────────────────────-──┘
          │                                          ▲
┌─────────▼─────────┐                               │
│  External Payment  │          ┌──────────────────-─┴───────────────────┐
│  （第三方支付）     │          │          Notification Service           │
└───────────────────┘          │  - 發送郵件、簡訊                        │
                               │  - @Async + 訂閱 Kafka 事件             │
                               └────────────────────────────────────────┘

          ┌──────────────────────────────────────────────────────────────┐
          │                     共用基礎設施                              │
          │   Redis（快取/Session）  Eureka（服務發現）  Zipkin（追蹤）   │
          └──────────────────────────────────────────────────────────────┘
```

---

## 各服務詳細設計

### 1. User Service（使用者服務）

**知識應用**：[[05-Spring-Data-JPA]]、[[06-Spring-Security]]、[[11-驗證-Validation]]、[[10-異常處理-Exception-Handling]]

**主要功能**：使用者註冊、登入、個人資料管理、JWT Token 發行

```java
// 依賴清單（pom.xml）
// spring-boot-starter-web
// spring-boot-starter-data-jpa
// spring-boot-starter-security
// spring-boot-starter-validation
// spring-boot-starter-actuator
// jjwt（JWT）
// mysql-connector-j

// 1. Entity
@Entity
@Table(name = "users")
public class User {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(unique = true, nullable = false)
    private String username;

    @Column(unique = true, nullable = false)
    private String email;

    @Column(nullable = false)
    private String password;  // BCrypt 加密

    @Enumerated(EnumType.STRING)
    private UserRole role;

    private boolean active = true;

    @CreationTimestamp
    private LocalDateTime createdAt;

    // Getters and Setters...
}

// 2. Repository
@Repository
public interface UserRepository extends JpaRepository<User, Long> {
    Optional<User> findByUsername(String username);
    Optional<User> findByEmail(String email);
    boolean existsByUsername(String username);
    boolean existsByEmail(String email);
}

// 3. Service
@Service
@Transactional(readOnly = true)
public class UserService {

    private final UserRepository userRepository;
    private final PasswordEncoder passwordEncoder;
    private final ApplicationEventPublisher eventPublisher;

    // 建構子略

    @Transactional
    public User register(RegisterRequest request) {
        if (userRepository.existsByEmail(request.getEmail())) {
            throw new DuplicateEmailException(request.getEmail());
        }

        User user = new User();
        user.setUsername(request.getUsername());
        user.setEmail(request.getEmail());
        user.setPassword(passwordEncoder.encode(request.getPassword()));
        user.setRole(UserRole.USER);

        User saved = userRepository.save(user);

        // 發佈事件，Notification Service 監聽並發送歡迎郵件
        eventPublisher.publishEvent(new UserRegisteredEvent(this, saved));

        return saved;
    }
}

// 4. Auth Controller
@RestController
@RequestMapping("/api/auth")
public class AuthController {

    @PostMapping("/register")
    public ResponseEntity<ApiResponse<UserDTO>> register(
            @Valid @RequestBody RegisterRequest request) {
        User user = userService.register(request);
        return ResponseEntity.status(HttpStatus.CREATED)
            .body(ApiResponse.success(toDTO(user)));
    }

    @PostMapping("/login")
    public ResponseEntity<ApiResponse<LoginResponse>> login(
            @Valid @RequestBody LoginRequest request) {
        String token = authService.login(request);
        return ResponseEntity.ok(ApiResponse.success(new LoginResponse(token)));
    }

    @GetMapping("/me")
    public ResponseEntity<ApiResponse<UserDTO>> getCurrentUser(
            @AuthenticationPrincipal UserDetails userDetails) {
        User user = userService.findByUsername(userDetails.getUsername());
        return ResponseEntity.ok(ApiResponse.success(toDTO(user)));
    }
}
```

```yaml
# application.yml（User Service）
server:
  port: 8081

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/users_db
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: 86400000

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
```

---

### 2. Product Service（商品服務）

**知識應用**：[[05-Spring-Data-JPA]]、[[12-快取-Caching]]、[[11-驗證-Validation]]、[[15-AOP-切面導向程式設計]]

**主要功能**：商品 CRUD、庫存管理、分類管理、商品搜尋

```java
// 1. Entity
@Entity
@Table(name = "products")
public class Product {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(nullable = false)
    private String name;

    @Column(columnDefinition = "TEXT")
    private String description;

    @Column(precision = 10, scale = 2)
    private BigDecimal price;

    @Column(nullable = false)
    private int stock;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "category_id")
    private Category category;

    @Enumerated(EnumType.STRING)
    private ProductStatus status = ProductStatus.ACTIVE;

    // Getters and Setters...
}

// 2. Repository
@Repository
public interface ProductRepository extends JpaRepository<Product, Long> {

    @Query("SELECT p FROM Product p WHERE p.status = 'ACTIVE' " +
           "AND (:categoryId IS NULL OR p.category.id = :categoryId) " +
           "AND (:keyword IS NULL OR p.name LIKE %:keyword%)")
    Page<Product> searchProducts(
        @Param("categoryId") Long categoryId,
        @Param("keyword") String keyword,
        Pageable pageable);

    // 庫存不足的商品
    @Query("SELECT p FROM Product p WHERE p.stock < :threshold")
    List<Product> findLowStockProducts(@Param("threshold") int threshold);

    // 更新庫存（樂觀鎖）
    @Modifying
    @Query("UPDATE Product p SET p.stock = p.stock - :quantity " +
           "WHERE p.id = :id AND p.stock >= :quantity")
    int decreaseStock(@Param("id") Long id, @Param("quantity") int quantity);
}

// 3. Service（帶快取）
@Service
@Transactional(readOnly = true)
public class ProductService {

    private final ProductRepository productRepository;

    // 查詢商品（快取 60 分鐘）
    @Cacheable(value = "products", key = "#id")
    public ProductDTO findById(Long id) {
        return productRepository.findById(id)
            .map(this::toDTO)
            .orElseThrow(() -> new ResourceNotFoundException("Product", "id", id));
    }

    // 搜尋商品（分頁）
    @Cacheable(value = "product-search",
               key = "#categoryId + '_' + #keyword + '_' + #page + '_' + #size")
    public Page<ProductDTO> searchProducts(Long categoryId, String keyword, int page, int size) {
        PageRequest pageable = PageRequest.of(page, size, Sort.by("name").ascending());
        return productRepository.searchProducts(categoryId, keyword, pageable)
            .map(this::toDTO);
    }

    // 更新商品（清除快取）
    @Transactional
    @CachePut(value = "products", key = "#id")
    @CacheEvict(value = "product-search", allEntries = true)
    public ProductDTO updateProduct(Long id, UpdateProductRequest request) {
        Product product = productRepository.findById(id)
            .orElseThrow(() -> new ResourceNotFoundException("Product", "id", id));
        // 更新邏輯...
        return toDTO(productRepository.save(product));
    }

    // 扣減庫存（訂單建立時呼叫）
    @Transactional
    @CacheEvict(value = "products", key = "#productId")
    public void decreaseStock(Long productId, int quantity) {
        int updated = productRepository.decreaseStock(productId, quantity);
        if (updated == 0) {
            throw new InsufficientStockException(productId, quantity);
        }
    }
}

// 4. 庫存不足定時檢查（搭配 @Scheduled）
@Component
public class InventoryMonitor {

    private final ProductRepository productRepository;
    private final ApplicationEventPublisher eventPublisher;

    @Scheduled(cron = "0 0 9 * * MON-FRI")  // 每個工作日早上 9 點
    public void checkLowStock() {
        List<Product> lowStockProducts = productRepository.findLowStockProducts(10);

        if (!lowStockProducts.isEmpty()) {
            eventPublisher.publishEvent(new LowStockEvent(this, lowStockProducts));
        }
    }
}
```

---

### 3. Order Service（訂單服務）

**知識應用**：[[05-Spring-Data-JPA]]、[[14-非同步處理-Async]]、[[13-排程任務-Scheduling]]、[[15-AOP-切面導向程式設計]]

**主要功能**：建立訂單、訂單狀態管理、訂單查詢

```java
// 1. Entity
@Entity
@Table(name = "orders")
public class Order {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(nullable = false)
    private Long userId;

    @Enumerated(EnumType.STRING)
    private OrderStatus status = OrderStatus.PENDING_PAYMENT;

    @Column(precision = 10, scale = 2)
    private BigDecimal totalAmount;

    @OneToMany(cascade = CascadeType.ALL, mappedBy = "order")
    private List<OrderItem> items = new ArrayList<>();

    @Column(updatable = false)
    private LocalDateTime createdAt;

    private LocalDateTime updatedAt;

    @PrePersist
    protected void onCreate() {
        createdAt = LocalDateTime.now();
        updatedAt = LocalDateTime.now();
    }

    @PreUpdate
    protected void onUpdate() {
        updatedAt = LocalDateTime.now();
    }
}

@Entity
@Table(name = "order_items")
public class OrderItem {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne
    @JoinColumn(name = "order_id")
    private Order order;

    private Long productId;
    private String productName;  // 快照（防止商品資訊變更）
    private int quantity;

    @Column(precision = 10, scale = 2)
    private BigDecimal unitPrice;   // 快照（防止價格變動影響歷史訂單）
}

// 2. Service
@Service
@Transactional(readOnly = true)
public class OrderService {

    private final OrderRepository orderRepository;
    private final ProductServiceClient productClient;  // Feign Client
    private final ApplicationEventPublisher eventPublisher;

    @Transactional
    @AuditLog(action = "建立訂單", module = "訂單管理")  // 自定義 AOP 注解
    public Order createOrder(Long userId, CreateOrderRequest request) {
        Order order = new Order();
        order.setUserId(userId);

        BigDecimal total = BigDecimal.ZERO;

        for (OrderItemRequest itemReq : request.getItems()) {
            // 呼叫 Product Service 取得商品資訊
            ProductDTO product = productClient.getProduct(itemReq.getProductId());

            // 扣減庫存
            productClient.decreaseStock(itemReq.getProductId(), itemReq.getQuantity());

            OrderItem item = new OrderItem();
            item.setOrder(order);
            item.setProductId(product.getId());
            item.setProductName(product.getName());   // 快照
            item.setUnitPrice(product.getPrice());    // 快照
            item.setQuantity(itemReq.getQuantity());
            order.getItems().add(item);

            total = total.add(product.getPrice().multiply(
                BigDecimal.valueOf(itemReq.getQuantity())));
        }

        order.setTotalAmount(total);
        Order saved = orderRepository.save(order);

        // 發佈訂單事件（非同步處理通知、支付等）
        eventPublisher.publishEvent(new OrderPlacedEvent(this, saved));

        return saved;
    }

    // 分頁查詢使用者的訂單
    public Page<OrderDTO> findUserOrders(Long userId, int page, int size) {
        PageRequest pageable = PageRequest.of(page, size,
            Sort.by("createdAt").descending());
        return orderRepository.findByUserId(userId, pageable)
            .map(this::toDTO);
    }
}

// 3. 超時訂單排程
@Component
public class OrderScheduler {

    private final OrderRepository orderRepository;
    private final ApplicationEventPublisher eventPublisher;

    // 每 5 分鐘檢查超時未付款的訂單
    @Scheduled(fixedDelay = 300000)
    @Transactional
    public void cancelExpiredOrders() {
        LocalDateTime expireTime = LocalDateTime.now().minusMinutes(30);

        List<Order> expired = orderRepository.findByStatusAndCreatedAtBefore(
            OrderStatus.PENDING_PAYMENT, expireTime);

        expired.forEach(order -> {
            order.setStatus(OrderStatus.CANCELLED);
            eventPublisher.publishEvent(new OrderCancelledEvent(this, order));
        });

        if (!expired.isEmpty()) {
            orderRepository.saveAll(expired);
        }
    }
}
```

---

### 4. Notification Service（通知服務）

**知識應用**：[[14-非同步處理-Async]]、[[08-日誌管理-Logging]]

**主要功能**：監聽各服務事件，非同步發送郵件、簡訊通知

```java
@Component
public class OrderEventListener {

    private final EmailService emailService;
    private static final Logger log = LoggerFactory.getLogger(OrderEventListener.class);

    @Async("emailExecutor")
    @KafkaListener(topics = "order.placed", groupId = "notification-service")
    public void handleOrderPlaced(OrderPlacedEvent event) {
        log.info("處理訂單建立通知：orderId={}", event.getOrderId());

        emailService.sendOrderConfirmation(
            event.getUserEmail(),
            event.getOrderId(),
            event.getTotalAmount()
        );
    }

    @Async("emailExecutor")
    @KafkaListener(topics = "order.cancelled")
    public void handleOrderCancelled(OrderCancelledEvent event) {
        log.info("處理訂單取消通知：orderId={}", event.getOrderId());
        emailService.sendOrderCancellation(event.getUserEmail(), event.getOrderId());
    }
}
```

---

## 服務間通訊

### 同步通訊：OpenFeign（HTTP）

```xml
<!-- Spring Cloud OpenFeign -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-openfeign</artifactId>
</dependency>
```

```java
// 在 Order Service 中宣告 Feign Client
@FeignClient(name = "product-service", url = "${services.product.url}")
public interface ProductServiceClient {

    @GetMapping("/api/products/{id}")
    ProductDTO getProduct(@PathVariable Long id);

    @PostMapping("/api/products/{id}/stock/decrease")
    void decreaseStock(@PathVariable Long id, @RequestParam int quantity);
}
```

### 非同步通訊：Kafka（事件）

```xml
<!-- Spring Kafka -->
<dependency>
    <groupId>org.springframework.kafka</groupId>
    <artifactId>spring-kafka</artifactId>
</dependency>
```

```java
// 發佈事件（Order Service）
@Service
public class OrderEventPublisher {

    private final KafkaTemplate<String, Object> kafkaTemplate;

    public void publishOrderPlaced(Order order) {
        kafkaTemplate.send("order.placed", order.getId().toString(),
            new OrderPlacedEvent(order));
    }
}

// 訂閱事件（Notification Service）
@Component
public class OrderEventConsumer {

    @KafkaListener(topics = "order.placed", groupId = "notification-group")
    public void handleOrderPlaced(OrderPlacedEvent event) {
        // 發送通知...
    }
}
```

---

## API Gateway 設定

```yaml
# Spring Cloud Gateway 設定
spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://user-service      # lb:// 使用服務發現
          predicates:
            - Path=/api/auth/**, /api/users/**
          filters:
            - name: CircuitBreaker    # 熔斷器
              args:
                name: userService
                fallbackUri: forward:/fallback

        - id: product-service
          uri: lb://product-service
          predicates:
            - Path=/api/products/**, /api/categories/**
          filters:
            - name: RequestRateLimiter  # 限流
              args:
                redis-rate-limiter.replenishRate: 100
                redis-rate-limiter.burstCapacity: 200

        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/api/orders/**
          filters:
            - JwtAuthentication        # 自定義 JWT 驗證 Filter
```

---

## 監控與可觀測性

**知識應用**：[[07-Spring-Boot-Actuator]]、[[08-日誌管理-Logging]]

每個服務都加入：

```xml
<!-- Actuator + Prometheus -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
<dependency>
    <groupId>io.micrometer</groupId>
    <artifactId>micrometer-registry-prometheus</artifactId>
</dependency>
<!-- 分散式追蹤 -->
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-sleuth</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-sleuth-zipkin</artifactId>
</dependency>
```

```properties
# 每個服務的 application.properties
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.metrics.tags.application=${spring.application.name}

# Zipkin 追蹤
spring.sleuth.sampler.probability=1.0
spring.zipkin.base-url=http://zipkin:9411
```

---

## Docker Compose 一鍵啟動

```yaml
# docker-compose.yml

version: '3.8'

services:
  # === 基礎設施 ===
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./sql:/docker-entrypoint-initdb.d  # 初始化腳本建立各 DB
    ports:
      - "3306:3306"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  kafka:
    image: confluentinc/cp-kafka:latest
    environment:
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
    depends_on:
      - zookeeper

  zookeeper:
    image: confluentinc/cp-zookeeper:latest
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181

  zipkin:
    image: openzipkin/zipkin
    ports:
      - "9411:9411"

  # === 微服務 ===
  user-service:
    build: ./user-service
    ports:
      - "8081:8081"
    environment:
      DB_URL: jdbc:mysql://mysql:3306/users_db
      DB_USER: root
      DB_PASSWORD: root
      JWT_SECRET: mySecretKey123456789012345678901234
    depends_on:
      - mysql

  product-service:
    build: ./product-service
    ports:
      - "8082:8082"
    environment:
      DB_URL: jdbc:mysql://mysql:3306/products_db
      SPRING_REDIS_HOST: redis
    depends_on:
      - mysql
      - redis

  order-service:
    build: ./order-service
    ports:
      - "8083:8083"
    environment:
      DB_URL: jdbc:mysql://mysql:3306/orders_db
      SERVICES_PRODUCT_URL: http://product-service:8082
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
    depends_on:
      - mysql
      - kafka
      - product-service

  notification-service:
    build: ./notification-service
    environment:
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
    depends_on:
      - kafka

  api-gateway:
    build: ./api-gateway
    ports:
      - "8080:8080"
    environment:
      SPRING_REDIS_HOST: redis
    depends_on:
      - user-service
      - product-service
      - order-service
```

---

## 本系列知識地圖

以下是本文用到的所有概念與對應文章：

| 技術/功能 | 應用服務 | 對應文章 |
|-----------|---------|---------|
| REST API 設計 | 所有服務 | [[04-Spring-MVC-RESTful-API]] |
| Spring Data JPA | 所有服務 | [[05-Spring-Data-JPA]] |
| JWT 認證 | User Service, API Gateway | [[06-Spring-Security]] |
| 快取（Redis/Caffeine） | Product Service | [[12-快取-Caching]] |
| 排程任務 | Order Service, Product Service | [[13-排程任務-Scheduling]] |
| 非同步處理 | Notification Service | [[14-非同步處理-Async]] |
| AOP（稽核日誌） | Order Service | [[15-AOP-切面導向程式設計]] |
| 驗證 | 所有服務 | [[11-驗證-Validation]] |
| 異常處理 | 所有服務 | [[10-異常處理-Exception-Handling]] |
| 多環境配置 | 所有服務 | [[03-應用程式配置-Properties-and-YAML]] |
| Actuator + Prometheus | 所有服務 | [[07-Spring-Boot-Actuator]] |
| 結構化日誌 | 所有服務 | [[08-日誌管理-Logging]] |
| Docker 部署 | 所有服務 | [[17-打包與部署]] |
| 測試策略 | 所有服務 | [[09-測試-Testing]] |

---

## 學習路徑建議

```
初學者入門
    ↓
[[00-Spring-Boot-2-簡介與快速入門]]
    ↓
[[03-應用程式配置-Properties-and-YAML]]
    ↓
[[04-Spring-MVC-RESTful-API]]
    ↓
[[05-Spring-Data-JPA]]
    ↓
[[10-異常處理-Exception-Handling]] + [[11-驗證-Validation]]
    ↓
[[06-Spring-Security]]（JWT）
    ↓
進階功能
[[12-快取-Caching]] → [[13-排程任務-Scheduling]] → [[14-非同步處理-Async]]
    ↓
底層原理
[[01-自動配置-Auto-Configuration]] → [[15-AOP-切面導向程式設計]]
    ↓
生產就緒
[[07-Spring-Boot-Actuator]] → [[08-日誌管理-Logging]] → [[17-打包與部署]]
    ↓
架構選擇
[[19-電商微服務系統-實戰總結]]（本文）←→ [[20-電商系統-Spring-Modulith-版本]]
```

---

## 微服務 vs 模組化單體

> 看完本文後，強烈建議閱讀 [[20-電商系統-Spring-Modulith-版本]]，了解同一個電商系統以 **Spring Modulith** 架構實作的版本，並比較兩種架構的優缺點。

| 面向 | 微服務（本文） | Spring Modulith |
|------|--------------|----------------|
| 部署複雜度 | 高（多個服務） | 低（單一應用） |
| 開發複雜度 | 高（服務間通訊） | 低（同一個 JVM） |
| 擴展性 | 高（個別服務可獨立擴展） | 中（整體擴展） |
| 適合團隊規模 | 大型團隊（多個小團隊） | 中小型團隊 |
| 資料一致性 | 難（分散式事務） | 容易（同一個 DB） |
| 初期成本 | 高 | 低 |
